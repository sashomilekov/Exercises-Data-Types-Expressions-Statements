function solve(n) {
    var binary = parseInt(n, 2) ; // code for 104
    var digit = parseInt(binary, 10); // Convert String or Digit (But it does not work !)
    console.log(binary);
}
solve('00001001');
